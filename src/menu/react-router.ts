import { IMenu } from "@/types/app";

const menu: IMenu = {
  title: "React Router",
  path: "react-router",
  children: [
    {
      title: "Basic Demo",
      path: "basic-demo",
    },
  ],
};

export default menu;
